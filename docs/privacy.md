# Privacy Notes

This public repository does not collect telemetry.

The Vixy CLI may store local configuration and session data under `~/.vixy/`.
Do not upload that directory to public issues. Before sharing logs, remove:

- API keys
- Personal access tokens
- Private source code
- Customer data
- Session transcripts
- Absolute paths that reveal sensitive project names
