# Public Repository Policy

This repository exists so users can install Vixy, read release notes, verify
checksums, and report public distribution issues.

Do not add:

- Private Vixy source code
- Internal architecture documents
- Build logs with private paths
- API keys, tokens, or credentials
- Customer data
- Release binaries

Release binaries must be uploaded to the authorized release host, not committed
to this repository.
